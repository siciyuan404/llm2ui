/**
 * AspectRatio Component
 * 
 * 宽高比组件，用于保持内容的固定宽高比。
 * 基于 Radix UI AspectRatio 构建，支持任意比例值。
 * 
 * @module components/ui/aspect-ratio
 */

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
