/**
 * @file tool-call.tsx
 * @description Tool call 组件占位文件
 * @module components/ui/tool-call
 */

import * as React from "react"

// 占位组件，待实现
export function ToolCall({ children }: { children?: React.ReactNode }) {
  return <div>{children}</div>
}

export default ToolCall
